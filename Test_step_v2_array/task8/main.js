const car = {type:'sedan', color:'green', engine:2.5};
const carDescription = `${car.type} ${car.color} ${car.engine}`;
console.log(carDescription);
const color = {red:100, green:0, blue:50};
const referenceColor = `${color.red = 50} ${color.green = 50} ${color.blue}`
console.log(referenceColor);